server:
  port: 8761

spring:
  application:
    name: discovery-server

# Eureka 서버 자신은 레지스트리에 등록하거나 다른 서버를 조회하지 않는다(단일 노드).
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
