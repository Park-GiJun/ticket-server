package com.gijun.ticketserver.infrastructure.adapter.out.payment.persistence.entity

// TODO: JPA 엔티티 정의.
//   @Entity @Table(name = "payments") + @Id, 컬럼 매핑, toModel()/fromModel() 변환.
//   (지금은 @Entity 미선언 — @Id 없는 엔티티가 스캔되면 부팅이 깨지므로 구현 시 함께 추가할 것)
